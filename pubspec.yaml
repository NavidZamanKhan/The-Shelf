name: the_shelf
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  read_pdf_text: ^0.3.0
  file_picker: ^8.1.7
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5
  phosphoricons_flutter: ^1.0.0
  shared_preferences: ^2.5.2
  sqflite: ^2.4.1
  sqflite_common_ffi: ^2.3.4
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/models/tfidf_model.json
